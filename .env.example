# Browser-facing API base (no trailing slash).
# Leave unset/localhost when browsing via LAN IP — the client then uses
# the same hostname as the page with port 8080 (e.g. http://192.168.x.x:8080).
NEXT_PUBLIC_API_URL=http://localhost:8080

# Optional server-side default
API_URL=http://localhost:8080
