# Upstream API used by Next.js rewrites (/backend/* → API).
# On the server this should reach the API from the web process (often localhost).
API_URL=http://127.0.0.1:8080

# Optional direct browser API URL (unused when same-origin /backend proxy is active)
NEXT_PUBLIC_API_URL=http://127.0.0.1:8080
