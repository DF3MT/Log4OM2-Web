services:
  web:
    build:
      context: .
      args:
        NEXT_PUBLIC_API_URL: http://localhost:8080
    ports:
      - "3000:3000"
    environment:
      NODE_ENV: production
    # API is expected on the host or via separate compose (Log4OM2-API)
