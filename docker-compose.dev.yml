services:
  web:
    image: ghcr.io/df3mt/log4om-web:latest
    build:
      context: .
    ports:
      - "3000:3000"
    environment:
      NODE_ENV: production
      API_URL: http://api:8080
    restart: unless-stopped
